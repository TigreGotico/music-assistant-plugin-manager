name: Release Preview
on:
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  release_preview:
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@dev
    with:
      version_file: "music_assistant_plugin_manager/version.py"
    secrets: inherit
